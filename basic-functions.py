# Show the parameters into the command line
print('example') # > "example"
print(2) # > 2

# Return the datatype of the parameters
type('hello world') # > class 'str'

# Len, return the length of the object
x = 'object'
len(x) # > 6
